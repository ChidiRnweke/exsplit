{   
    type: database-config,
    url = jdbc:postgresql://localhost:5432/mydatabase
    user = myuser
    password = mypassword
    migrationsTable = migrations
    migrationsLocations = [
        db/migrations
    ]
}